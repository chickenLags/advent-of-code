{
  "devDependencies": {
    "@types/node": "^16.11.11",
    "tslint": "^6.1.3",
    "typescript": "^4.5.2"
  },
  "scripts": {
    "build": "npx tsc -p tsconfig.json",
    "start": "npm run build && node index.js",
    "watch": "npx tsc -w"
  },
  "dependencies": {
    "@types/es6-promise": "^3.3.0",
    "axios": "^0.24.0"
  }
}
